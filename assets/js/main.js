var scrollOptions = {
  mode: 'set',
  sectionScroll: false,
  initialScroll: true,
  keepHistory: false,
  sectionWrapperSelector: '.section-wrapper',
  sectionClass: 'section-wrapper__section',
  animationSpeed: 500,
  headerHash: 'header',
  breakpoint: null,
  eventEmitter: null,
  dynamicHeight: true
};

(function () {
  'use strict';

  // $.smartscroll(scrollOptions);

})();